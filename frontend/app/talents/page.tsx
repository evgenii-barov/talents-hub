import { SectionPlaceholder } from "@/components/layout/section-placeholder";

export default function TalentsPage() {
  return (
    <SectionPlaceholder
      eyebrow="TALENT"
      title="Find people to build with."
      description="The public talent catalogue is now available through the API and will be connected to this page in the next UI increment."
    />
  );
}

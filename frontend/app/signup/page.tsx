import { SectionPlaceholder } from "@/components/layout/section-placeholder";

export default function SignupPage() {
  return (
    <SectionPlaceholder
      eyebrow="CREATE PROFILE"
      title="Create your Talents Hub profile."
      description="The profile API is ready. This screen will collect account details and profile information once the authentication interface is connected."
    />
  );
}

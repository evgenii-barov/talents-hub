import { SectionPlaceholder } from "@/components/layout/section-placeholder";

export default function ProjectsPage() {
  return (
    <SectionPlaceholder
      eyebrow="PROJECTS"
      title="Purposeful projects, in one place."
      description="The project catalogue will be connected once project creation, moderation, and roles are implemented in the API."
    />
  );
}

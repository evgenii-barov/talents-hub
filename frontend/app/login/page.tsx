import { SectionPlaceholder } from "@/components/layout/section-placeholder";

export default function LoginPage() {
  return (
    <SectionPlaceholder
      eyebrow="SIGN IN"
      title="Your account will live here."
      description="Email and OAuth authentication are configured on the backend. The dedicated sign-in form will be connected in the authentication increment."
    />
  );
}
